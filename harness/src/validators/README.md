# Validator Ownership

G0.2 owns deterministic schema and reference validation against the published bundle. Later slices extend validation for freshness, research quality, decision readiness, adaptation policy, and report consistency without hiding semantic model judgment inside scripts.

The G0.1 repository doctor checks only skeleton and toolchain constraints. It must not be described or reused as a research artifact validator.
