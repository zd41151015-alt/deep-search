# G1.2 synthetic vertical fixture

本目录只包含 deterministic contract fixture。所有 source、raw bytes、Claim、Finding 和 Insight 都是 synthetic，专门验证 Store publication、typed traceability、checkpoint 与 recovery；它们不是市场 Evidence，不证明任何 Startup Opportunity thesis，也不代表已完成外部 validation。
