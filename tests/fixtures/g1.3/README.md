# G1.3 synthetic fixtures

These fixtures exercise only deterministic Harness contracts. They do not contain market Evidence
or successful external validation. Public-source identifiers use `.invalid`; user material uses the
reserved `urn:startup-opportunity:user-provided:` namespace. Materialized Evidence remains
`model_inference_only`, `unverified`, and carries explicit limitations.
